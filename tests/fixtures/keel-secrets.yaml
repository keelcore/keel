# tests/fixtures/keel-secrets.yaml
# Stub secrets file for the P3 compose harness.
# In production this file is bind-mounted from a k8s Secret.
#
# Values here override keel.yaml; only non-empty fields take effect.
# This file is intentionally minimal — tests that need authn load
# a separate fixture via KEEL_SECRETS override.

# authn:
#   enabled: true
#   trusted_ids:
#     - test-client
#   trusted_signers:
#     - "test-hmac-secret-32-bytes-minimum"
#   my_id: keel-test